% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{pmp}
\alias{pmp}
\alias{pmp.badp_bma}
\title{Extract Posterior Model Probabilities}
\usage{
pmp(x, ...)

\method{pmp}{badp_bma}(x, prior = c("binomial", "beta"), top = NULL, ...)
}
\arguments{
\item{x}{An object of class \code{badp_bma}.}

\item{...}{Arguments passed to methods.}

\item{prior}{Model prior: \code{"binomial"} (the default) or \code{"beta"}.}

\item{top}{Optional number of models to return, ordered by decreasing
posterior probability. By default every model is returned, unsorted.}
}
\value{
A numeric vector of posterior model probabilities, one per model,
in the order in which the models are held in the model space unless
\code{top} is given.
}
\description{
Returns the posterior probability of each model in the model space.
}
\examples{
\donttest{
data(full_model_space)
results <- bma(full_model_space)

sum(pmp(results))        # sums to one
pmp(results, top = 5)    # the five most probable models
}

}
\seealso{
\code{\link{pip}}, \code{\link{best_models}},
\code{\link{model_pmp}}
}
