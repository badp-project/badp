% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_dens.R
\name{posterior_dens}
\alias{posterior_dens}
\title{Graphs of the posterior densities of the coefficients}
\arguments{
\item{bma_list}{bma object (the result of the bma function)}

\item{prior}{Parameter indicating which model prior should be used for calculations:
\enumerate{
\item "binomial" - using binomial model prior (default option) \cr
\item "beta" - using binomial-beta model prior
}}

\item{SE}{Parameter indicating which standard errors should be used in calculation of posterior standard deviation: \cr
\enumerate{
\item "standard" - regular standard errors (default option)\cr
\item "robust" - robust standard errors
}}
}
\value{
A list with the graphs of the posterior densities of coefficients for all the considered regressors.
}
\description{
This function draws graphs of the posterior densities of all the coeffcients of interest.
}
\examples{
\donttest{
library(magrittr)

data_prepared <- badp::economic_growth[, 1:6] \%>\%
  badp::feature_standardization(
    excluded_cols = c(country, year, gdp)
  ) \%>\%
  badp::feature_standardization(
    group_by_col  = year,
    excluded_cols = country,
    scale         = FALSE
  )

bma_results <- bma(
  model_space = badp::small_model_space,
  round       = 3,
  dilution    = 0
)

posterior_graphs <- posterior_dens(bma_results, prior = "binomial", SE = "robust")
}
}
