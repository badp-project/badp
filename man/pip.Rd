% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{pip}
\alias{pip}
\alias{pip.badp_bma}
\title{Extract Posterior Inclusion Probabilities}
\usage{
pip(x, ...)

\method{pip}{badp_bma}(x, prior = c("binomial", "beta"), include_lagged = FALSE, ...)
}
\arguments{
\item{x}{An object of class \code{badp_bma}.}

\item{...}{Arguments passed to methods.}

\item{prior}{Model prior: \code{"binomial"} (the default) or \code{"beta"}.}

\item{include_lagged}{Logical. Include the lagged dependent variable, whose
inclusion probability is undefined. Defaults to \code{FALSE}.}
}
\value{
A named numeric vector of posterior inclusion probabilities.
}
\description{
Returns the posterior inclusion probability of each regressor.
}
\details{
The lagged dependent variable enters every model by construction and
therefore carries no inclusion probability. It is omitted unless
\code{include_lagged = TRUE}, in which case it appears first with value
\code{NA}.
}
\examples{
\donttest{
data(full_model_space)
results <- bma(full_model_space)

pip(results)
sort(pip(results), decreasing = TRUE)
}

}
\seealso{
\code{\link{bma_table}}, \code{\link{pmp}}, \code{\link{bma}}
}
